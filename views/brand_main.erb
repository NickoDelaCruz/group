<h1> BRAND MAIN PAGE (CHANGE)</h1>


<h4>BRANDS:</h4>

<br>

  <ul id="list">
    <% @brands.each do |brand| %>
      <li> <a href="/brands/<%= brand.id %>"><%= brand.name %></a> </li>
    <% end %>
  </ul>
<button id="add"> add </button>
